{
  "name": "performance-app-web",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-core": "node scripts/sync-core.mjs",
    "test": "npm run sync-core && node --test test/*.test.js",
    "dev": "npm run sync-core && npx wrangler dev",
    "deploy": "npm run sync-core && npx wrangler deploy"
  }
}
